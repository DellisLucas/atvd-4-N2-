class Professor{
    constructor(id,nome,formacao,idDisciplina)
    {
       this.id = id
       this.nome = nome
       this.formacao = formacao
       this.idDisciplina = idDisciplina
    }
}

module.exports = Professor;